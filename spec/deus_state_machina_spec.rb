require 'spec_helper'

describe DeusStateMachina do
  it 'has a version number' do
    expect(DeusStateMachina::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
